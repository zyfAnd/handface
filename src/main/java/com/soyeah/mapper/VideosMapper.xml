<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.soyeah.mapper.VideosMapper">
    <insert id="getVideos" useGeneratedKeys="true" keyProperty="id" parameterType="com.soyeah.model.entity.User"  >
        INSERT INTO T_USER (openid,username,imageUrl,point,city,country,province,gender,form_id)
        VALUES (#{openid},#{username},#{imageUrl},#{point},#{city},#{country},#{province},#{gender},#{form_id})
    </insert>

</mapper>